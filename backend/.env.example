NODE_ENV=development
PORT=4000

# PostgreSQL
DATABASE_URL=postgresql://username:password@host:port/notes_app_db

# JWT (replace with a strong, random secret before any real deployment)
JWT_SECRET=any-random-string-you-want-to-use-as-a-secret
JWT_EXPIRES_IN=1h

# Logging
LOG_LEVEL=info
