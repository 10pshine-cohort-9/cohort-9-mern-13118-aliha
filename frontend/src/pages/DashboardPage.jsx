// TODO (Sprint 3): fetch GET /api/notes for the authenticated user,
// render the list, and link each note + a "New Note" button to
// /notes/:id and /notes/new respectively.
export default function DashboardPage() {
  return (
    <main>
      <h1>Dashboard</h1>
    </main>
  );
}
