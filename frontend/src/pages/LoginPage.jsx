// TODO (Sprint 3): wire this form against POST /api/auth/login,
// store the returned JWT, redirect to /dashboard on success.
export default function LoginPage() {
  return (
    <main>
      <h1>Log In</h1>
    </main>
  );
}
