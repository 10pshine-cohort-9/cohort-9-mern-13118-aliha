// TODO (Sprint 3): rich-text editor; POST /api/notes for a new note,
// PUT /api/notes/:id for an existing one, cancel returns to /dashboard.
export default function NoteEditorPage() {
  return (
    <main>
      <h1>Note Editor</h1>
    </main>
  );
}
