// TODO (Sprint 3): wire this form against POST /api/auth/signup,
// then redirect to /login (or auto-login) on success.
export default function SignupPage() {
  return (
    <main>
      <h1>Sign Up</h1>
    </main>
  );
}
